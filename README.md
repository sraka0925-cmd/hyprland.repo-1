# hyprland.repo-1
repo for both .conf and .lua
Check what version of hyprland you have (chcek ic config file is "hyprland.conf" or "hyprland.lua") and then, copy and paste the files with the exact end (.lua or .conf)
