-- femboy_hyprland.lua
-- Cute pastel Hyprland config generator for Arch Linux.
--
-- Usage:
--   lua femboy_hyprland.lua > ~/.config/hypr/hyprland.conf
--
-- Optional packages that match this config:
--   hyprland waybar kitty wofi mako swww grim slurp wl-clipboard
--   xdg-desktop-portal-hyprland hyprpolkitagent qt5-wayland qt6-wayland

local cfg = {
  mod = "SUPER",
  terminal = "kitty",
  launcher = "wofi --show drun",
  browser = "firefox",
  file_manager = "dolphin",
  lock_cmd = "hyprlock",
  wallpaper_cmd = "swww-daemon && swww img ~/Pictures/wallpapers/cute.png --transition-type grow --transition-duration 1.2",
  monitor = ",preferred,auto,1",
  keyboard_layout = "pl",
}

local palette = {
  bg = "0xff1b1724",
  panel = "0xff2a2035",
  text = "0xffffeff8",
  pink = "0xffff8ac7",
  hot_pink = "0xffff5fbf",
  lavender = "0xffc9a7ff",
  blue = "0xff9bdcff",
  mint = "0xffa8ffd6",
  peach = "0xffffc2a6",
  yellow = "0xfffff1a8",
}

local function emit(lines)
  for _, line in ipairs(lines) do
    print(line)
  end
end

emit({
  "# Generated by femboy_hyprland.lua",
  "# Pastel, soft, cute Hyprland setup for Arch Linux.",
  "",
  "# ---------- Monitor ----------",
  "monitor = " .. cfg.monitor,
  "",
  "# ---------- Programs ----------",
  "$mod = " .. cfg.mod,
  "$terminal = " .. cfg.terminal,
  "$launcher = " .. cfg.launcher,
  "$browser = " .. cfg.browser,
  "$fileManager = " .. cfg.file_manager,
  "",
  "# ---------- Autostart ----------",
  "exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP",
  "exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP",
  "exec-once = hyprpolkitagent",
  "exec-once = waybar",
  "exec-once = mako",
  "exec-once = " .. cfg.wallpaper_cmd,
  "",
  "# ---------- Environment ----------",
  "env = XDG_CURRENT_DESKTOP,Hyprland",
  "env = XDG_SESSION_TYPE,wayland",
  "env = XDG_SESSION_DESKTOP,Hyprland",
  "env = QT_QPA_PLATFORM,wayland;xcb",
  "env = GDK_BACKEND,wayland,x11",
  "env = CLUTTER_BACKEND,wayland",
  "env = SDL_VIDEODRIVER,wayland",
  "env = MOZ_ENABLE_WAYLAND,1",
  "env = NIXOS_OZONE_WL,1",
  "",
  "# ---------- Input ----------",
  "input {",
  "    kb_layout = " .. cfg.keyboard_layout,
  "    follow_mouse = 1",
  "    sensitivity = 0",
  "    touchpad {",
  "        natural_scroll = true",
  "        tap-to-click = true",
  "        clickfinger_behavior = true",
  "        scroll_factor = 0.45",
  "    }",
  "}",
  "",
  "gestures {",
  "    workspace_swipe = true",
  "    workspace_swipe_fingers = 3",
  "    workspace_swipe_distance = 280",
  "}",
  "",
  "# ---------- Look: pastel femboy mode ----------",
  "general {",
  "    gaps_in = 6",
  "    gaps_out = 14",
  "    border_size = 3",
  "    col.active_border = rgba(ff8ac7ff) rgba(c9a7ffff) rgba(9bdcffff) 45deg",
  "    col.inactive_border = rgba(4a3a5a99)",
  "    layout = dwindle",
  "    allow_tearing = false",
  "}",
  "",
  "decoration {",
  "    rounding = 18",
  "    active_opacity = 0.96",
  "    inactive_opacity = 0.84",
  "    fullscreen_opacity = 1.0",
  "    shadow {",
  "        enabled = true",
  "        range = 22",
  "        render_power = 4",
  "        color = rgba(ff8ac766)",
  "        color_inactive = rgba(1b172455)",
  "    }",
  "    blur {",
  "        enabled = true",
  "        size = 8",
  "        passes = 3",
  "        new_optimizations = true",
  "        ignore_opacity = true",
  "        xray = false",
  "    }",
  "}",
  "",
  "animations {",
  "    enabled = true",
  "    bezier = softPop, 0.16, 1, 0.3, 1",
  "    bezier = candy, 0.22, 0.9, 0.28, 1.1",
  "    animation = windows, 1, 5, softPop, popin 85%",
  "    animation = windowsOut, 1, 4, softPop, popin 80%",
  "    animation = border, 1, 8, candy",
  "    animation = borderangle, 1, 9, candy",
  "    animation = fade, 1, 5, softPop",
  "    animation = workspaces, 1, 5, softPop, slide",
  "}",
  "",
  "dwindle {",
  "    pseudotile = true",
  "    preserve_split = true",
  "    smart_split = true",
  "}",
  "",
  "misc {",
  "    disable_hyprland_logo = true",
  "    disable_splash_rendering = true",
  "    mouse_move_enables_dpms = true",
  "    key_press_enables_dpms = true",
  "    vfr = true",
  "}",
  "",
  "# ---------- Window rules ----------",
  "windowrulev2 = float,class:^(pavucontrol)$",
  "windowrulev2 = float,class:^(blueman-manager)$",
  "windowrulev2 = float,class:^(nm-connection-editor)$",
  "windowrulev2 = float,title:^(Picture-in-Picture)$",
  "windowrulev2 = pin,title:^(Picture-in-Picture)$",
  "windowrulev2 = size 900 600,class:^(pavucontrol)$",
  "windowrulev2 = center,class:^(pavucontrol)$",
  "windowrulev2 = opacity 0.94 0.82,class:^(kitty)$",
  "windowrulev2 = opacity 0.96 0.88,class:^(wofi)$",
  "",
  "# ---------- Layers ----------",
  "layerrule = blur, waybar",
  "layerrule = blur, wofi",
  "layerrule = blur, notifications",
  "layerrule = ignorezero, waybar",
  "",
  "# ---------- Keybinds ----------",
  "bind = $mod, Return, exec, $terminal",
  "bind = $mod, Space, exec, $launcher",
  "bind = $mod, B, exec, $browser",
  "bind = $mod, E, exec, $fileManager",
  "bind = $mod, Q, killactive,",
  "bind = $mod SHIFT, Q, exit,",
  "bind = $mod, F, fullscreen,",
  "bind = $mod, V, togglefloating,",
  "bind = $mod, T, pseudo,",
  "bind = $mod, S, togglesplit,",
  "bind = $mod, Escape, exec, " .. cfg.lock_cmd,
  "",
  "# Move focus with vim-like keys",
  "bind = $mod, H, movefocus, l",
  "bind = $mod, L, movefocus, r",
  "bind = $mod, K, movefocus, u",
  "bind = $mod, J, movefocus, d",
  "",
  "# Move windows",
  "bind = $mod SHIFT, H, movewindow, l",
  "bind = $mod SHIFT, L, movewindow, r",
  "bind = $mod SHIFT, K, movewindow, u",
  "bind = $mod SHIFT, J, movewindow, d",
  "",
  "# Workspaces",
  "bind = $mod, 1, workspace, 1",
  "bind = $mod, 2, workspace, 2",
  "bind = $mod, 3, workspace, 3",
  "bind = $mod, 4, workspace, 4",
  "bind = $mod, 5, workspace, 5",
  "bind = $mod, 6, workspace, 6",
  "bind = $mod, 7, workspace, 7",
  "bind = $mod, 8, workspace, 8",
  "bind = $mod, 9, workspace, 9",
  "bind = $mod, 0, workspace, 10",
  "bind = $mod SHIFT, 1, movetoworkspace, 1",
  "bind = $mod SHIFT, 2, movetoworkspace, 2",
  "bind = $mod SHIFT, 3, movetoworkspace, 3",
  "bind = $mod SHIFT, 4, movetoworkspace, 4",
  "bind = $mod SHIFT, 5, movetoworkspace, 5",
  "bind = $mod SHIFT, 6, movetoworkspace, 6",
  "bind = $mod SHIFT, 7, movetoworkspace, 7",
  "bind = $mod SHIFT, 8, movetoworkspace, 8",
  "bind = $mod SHIFT, 9, movetoworkspace, 9",
  "bind = $mod SHIFT, 0, movetoworkspace, 10",
  "",
  "# Screenshots",
  "bind = , Print, exec, grim -g \"$(slurp)\" - | wl-copy",
  "bind = SHIFT, Print, exec, grim ~/Pictures/Screenshots/shot-$(date +%Y%m%d-%H%M%S).png",
  "",
  "# Media and brightness",
  "bind = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+",
  "bind = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-",
  "bind = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
  "bind = , XF86MonBrightnessUp, exec, brightnessctl set +8%",
  "bind = , XF86MonBrightnessDown, exec, brightnessctl set 8%-",
  "",
  "# Resize with mouse",
  "bindm = $mod, mouse:272, movewindow",
  "bindm = $mod, mouse:273, resizewindow",
  "",
  "# ---------- Cute workspace names for Waybar users ----------",
  "# Use these icons/names in your Waybar config if you want the full pastel vibe:",
  "# 1: pink, 2: lavender, 3: sky, 4: mint, 5: peach, 6: lemon, 7: candy, 8: plush, 9: star, 10: heart",
})

-- Theme values for other Lua-aware tools, if you want to reuse this file.
return {
  hyprland = cfg,
  palette = palette,
}
